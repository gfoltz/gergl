import * as React from 'react'
import ReactDOM from 'react-dom'
import './index.css'
import { GerglApp } from './GerglApp'

ReactDOM.render(
  <React.StrictMode>
    <GerglApp />
  </React.StrictMode>,
  document.getElementById('root')
)
